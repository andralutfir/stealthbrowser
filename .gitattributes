# Keep line endings sane across platforms. The shell scripts must stay LF or
# the shebang breaks on Linux; the .cmd files must stay CRLF for cmd.exe.
* text=auto eol=lf
*.sh   text eol=lf
*.cmd  text eol=crlf
*.exe  binary
*.png  binary
